use crate::Solution;
pub fn solve(_input: &str) -> Solution {
    // let forest: Vec<Vec<isize>> = input
    //     .lines()
    //     .map(|x| {
    //         x.chars()
    //             .map(|c| c.to_digit(10).expect("thats not a number") as isize)
    //             .collect()
    //     })
    //     .collect();

    // Solution::Isize(0, 0)
    todo!();
}
